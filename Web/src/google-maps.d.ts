/// <reference types="google.maps" />
// Carga global de los tipos oficiales de Google Maps JS API (@types/google.maps,
// devDependency). Habilita el namespace `google.maps.*` en todo el proyecto sin
// usar `any` (p. ej. el callback de geocodificación en GeolocalizadorView).
export {};
